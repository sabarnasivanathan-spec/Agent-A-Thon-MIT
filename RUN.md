# RUN.md

## Demo URL

https://agent-a-thon-mit-1.onrender.com

## Local demo command

Run this command from the repository root:

```bash
uvicorn main:app --reload --port 8000
```

For the hosted demo, open the Demo URL above.

For local execution, run the command from the folder containing `main.py`.
The SQLite database is initialized automatically when the backend starts.
